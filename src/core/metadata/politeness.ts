/**
 * Contact address sent to Crossref/OpenAlex/Unpaywall (`mailto=` / `email=`),
 * which admits requests to their faster "polite" pools and gives them someone
 * to contact instead of blocking if a bug ever floods them.
 */
export const MAILTO = "kkr36@cornell.edu";
